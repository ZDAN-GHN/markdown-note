## 1. 工作模式与沟通

### 1.1 回答风格
- **详细步骤**：提供解决方案时，请包含清晰的步骤说明，必要时附带代码示例。
- **多种方案**：对于设计决策，如果存在多个可行方案，请简要列出优缺点，并给出推荐选项。
- **时间预估**：对于任务或问题，可以附上预估的实现时间（如“预计需要 2 小时”），帮助评估工作量。
- **主动提问**：当需求不明确或存在歧义时，请主动提出问题以澄清，而非自行假设。

### 1.2 任务处理
- 任务优先级：按照用户指定的顺序处理；若无明确顺序，按依赖关系（先基础后扩展）处理。
- 任务受阻：如果遇到技术障碍或需要外部信息，请立即提出，并给出备选方案或暂停点。
- 更新进度：在完成任务后，简要总结完成内容和下一步建议。

---

## 2. 代码规范

### 2.1 语言与框架
- **TypeScript**：所有代码必须使用 TypeScript 编写，严格模式开启（`strict: true`）。
- **React**：使用函数组件和 Hooks，避免类组件。
- **样式**：优先使用 TailwindCSS 进行样式编写；如需自定义 CSS，使用 CSS Modules 或内联样式（极少数情况）。

### 2.2 代码风格
- **缩进**：2 空格，无 tabs。
- **引号**：字符串使用单引号，JSX 属性使用双引号。
- **分号**：语句末尾必须加分号。
- **命名规范**：
  - 变量/函数：`camelCase`
  - 组件/类：`PascalCase`
  - 常量：`UPPER_SNAKE_CASE`
  - 文件/文件夹：`kebab-case`（如 `note-list.tsx`）
- **ESLint**：使用项目中的 ESLint 配置（通常基于 `eslint-config-react-app` 或自定义规则）。
- **Prettier**：代码提交前必须通过 Prettier 格式化。

### 2.3 注释
- **函数/组件**：使用 JSDoc 格式描述功能、参数、返回值。
- **复杂逻辑**：在关键代码处添加行内注释解释意图。
- **TODO/FIXME**：使用标准标记，并附带简要说明。

---

## 3. 技术栈约束

- **核心依赖**：React 18+, TypeScript 5+, Vite, TailwindCSS。
- **状态管理**：使用 React Context + useReducer 或 Zustand（小项目优先 Context）。
- **路由**：本项目为单页应用，无需路由（除非后续需要多页面）。
- **第三方库引入**：
  - 必须优先使用轻量、维护良好的库。
  - 引入前需确认许可证兼容性，并评估体积影响。
  - 如果引入较大库，需在注释中说明原因。

---

## 4. 测试要求

- **单元测试**：使用 Vitest（与 Vite 集成）编写测试。测试文件与源文件同目录，命名为 `*.test.ts` 或 `*.spec.ts`。
- **组件测试**：使用 React Testing Library 进行组件交互测试。
- **覆盖率**：核心逻辑（如 Markdown 解析、数据存储）需达到 80% 以上覆盖率。
- **运行命令**：`npm run test` 执行所有测试。

---

## 5. 文档要求

- **代码注释**：公共函数、组件、自定义 Hook 必须有 JSDoc。
- **README 更新**：如果新增功能影响使用方式，必须同步更新 README.md。
- **架构决策**：对于重大技术选型或架构调整，需记录在 `docs/` 目录下的 ADR（架构决策记录）文件中。

---

## 6. 版本控制

- **Git 分支**：主分支为 `main`，功能开发使用 `feature/xxx` 分支，修复使用 `fix/xxx`。
- **提交信息**：遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范，例如：
  - `feat: 添加笔记列表右键菜单`
  - `fix: 修复滚动同步偏移问题`
  - `docs: 更新 AGENTS.md`
- **Pull Request**：合并前需确保通过所有测试，并解决冲突。

---

## 7. 安全与性能

- **XSS 防御**：所有用户输入内容在渲染前必须经过 DOMPurify 过滤。
- **敏感数据**：不得将用户笔记数据发送到任何外部服务器。
- **性能优化**：
  - 避免不必要的重渲染（使用 `React.memo`、`useMemo`、`useCallback`）。
  - 大数据列表使用虚拟滚动。
  - 代码分割：通过 Vite 的 `import()` 实现路由级别或组件级别懒加载。
- **可访问性**：尽量遵循 WCAG 2.1 标准，确保键盘可操作、语义化 HTML。

---

## 8. 沟通与反馈

- **主动报告**：当发现潜在问题（如性能瓶颈、安全漏洞）时，及时提出并给出改进建议。
- **备选方案**：如果某个需求实现成本过高，可提出替代方案并分析利弊。
- **学习与改进**：定期回顾代码质量，提出可以优化的地方。

---

## 9. 其他

- **移动端支持**：所有功能需在移动设备上可用，响应式布局由 CSS 媒体查询实现。
- **浏览器兼容**：支持最新两个版本的 Chrome、Firefox、Safari、Edge。
- **极客风格**：视觉上必须保持终端深色背景、荧光绿高亮、硬边框、等宽字体，请参考设计稿或需求文档中的描述。

---

请智能体严格遵守以上指令。如有疑问，随时向用户提出澄清。让我们高效协作，打造一款优秀的极客风格 Markdown 笔记编辑器！

